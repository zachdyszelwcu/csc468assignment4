version: '3.8'

services:
  mongodb:
    image: mongo:6
    container_name: mongo-db
    ports:
      - "27017:27017"

  web:
    build: ./app
    container_name: node-app
    ports:
      - "8080:8080"
    depends_on:
      - mongodb
